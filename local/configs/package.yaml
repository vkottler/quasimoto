---
name: quasimoto
version: "{{major}}.{{minor}}.{{patch}}"
description: A lossless audio generator.
entry: {{entry}}
requirements:
  - vcorelib
dev_requirements:
  - setuptools-wrapper
  - types-setuptools

commands:
  - name: gen
    description: "generate audio"
